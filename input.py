# Input function allows us to take input from the user. It takes a string as an argument, which is displayed as a prompt to the user. The function then waits for the user to enter some input and returns it as a string.

# Variables can be used to store the input from the user. In this example, we are storing the input in a variable called "name". We can then print the value of the variable to see what the user entered.
# name = input("What is your name? ")
# print(name)

# school = input("What is your school name? ")
# print(school)

father = input("What is your father's name? ")
print(father)


mother = input("What is your mother's name? ")
print(mother)